let hello = "Hello";
let world = "World";
let epicPhrase = hello + " " + world + "!";

let epicPhrase2 = `${hello} ${world}!, esto esta genial`;

console.log(epicPhrase2);

// Multi-line Strings

let lorem = "Esto es un string \n" + "esto es otra linea";
console.log(lorem);

let lorem2 = `Esto es un string
esta es la continuacion de la linea anterior`;
console.log(lorem2);
