const letters = ["a", "b", "c"];

letters.forEach((eachValue) => console.log("forEach " + eachValue));

const array = [2, 4, 5, 6];

const newArray = array.map((eachValue) => eachValue * 2);

console.log(newArray);
